import { SignalGroup } from './signal-group';
import { Queue } from '@blackglory/structures';
import { IChannel } from '../shared/types';
export declare class UnlimitedChannel<T> implements IChannel<T> {
    isClosed: boolean;
    enqueueSignalGroup: SignalGroup;
    buffer: Queue<T>;
    send(value: T): void;
    receive(): AsyncIterable<T>;
    close(): void;
}
export { ChannelClosedError } from '../shared/error';
