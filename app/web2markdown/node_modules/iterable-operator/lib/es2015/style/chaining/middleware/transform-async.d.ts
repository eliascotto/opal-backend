import { Subject } from '../subject';
import { AsyncIterableOperator } from '../async-iterable-operator';
export declare class TransformAsyncOperator<T, U extends Iterable<T> | AsyncIterable<T>> extends Subject<U> {
    transformAsync<T>(transformer: (iterable: U) => AsyncIterable<T>): AsyncIterableOperator<T>;
}
