import { Subject } from '../subject';
import { AsyncIterableOperator } from '../async-iterable-operator';
export declare class MapAsyncOperator<T, U extends Iterable<T> | AsyncIterable<T>> extends Subject<U> {
    mapAsync<U>(fn: (element: T, index: number) => U | PromiseLike<U>): AsyncIterableOperator<U>;
}
