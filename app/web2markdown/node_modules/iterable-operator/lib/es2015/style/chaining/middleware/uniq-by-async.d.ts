import { Subject } from '../subject';
import { AsyncIterableOperator } from '../async-iterable-operator';
export declare class UniqByAsyncOperator<T, U extends Iterable<T> | AsyncIterable<T>> extends Subject<U> {
    uniqByAsync<U>(fn: (element: T, index: number) => U | PromiseLike<U>): AsyncIterableOperator<T>;
}
