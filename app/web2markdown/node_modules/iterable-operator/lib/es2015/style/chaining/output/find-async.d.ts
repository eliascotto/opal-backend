import { Subject } from '../subject';
export declare class FindAsyncOperator<T, U extends Iterable<T> | AsyncIterable<T>> extends Subject<U> {
    findAsync(fn: (element: T, index: number) => boolean | PromiseLike<boolean>): Promise<T>;
}
