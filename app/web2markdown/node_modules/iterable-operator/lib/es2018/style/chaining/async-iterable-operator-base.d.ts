import { Subject } from './subject';
export declare class AsyncIterableOperatorBase<T> extends Subject<AsyncIterable<T>> implements AsyncIterable<T> {
    [Symbol.asyncIterator](): AsyncIterator<T, any, undefined>;
}
