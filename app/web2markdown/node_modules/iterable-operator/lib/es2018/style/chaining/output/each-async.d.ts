import { Subject } from '../subject';
export declare class EachAsyncOperator<T, U extends Iterable<T> | AsyncIterable<T>> extends Subject<U> {
    eachAsync(fn: (element: T, index: number) => unknown | PromiseLike<unknown>): Promise<void>;
}
