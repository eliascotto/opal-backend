import { IterableOperatorBase } from '../iterable-operator-base';
export declare class SomeOperator<T> extends IterableOperatorBase<T> {
    some(fn: (element: T, index: number) => boolean): boolean;
}
