import { AsyncIterableOperatorBase } from '../async-iterable-operator-base';
export declare class IncludesAsyncOperator<T> extends AsyncIterableOperatorBase<T> {
    includesAsync(value: T): Promise<boolean>;
}
