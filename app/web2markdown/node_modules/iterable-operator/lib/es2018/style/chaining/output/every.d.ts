import { IterableOperatorBase } from '../iterable-operator-base';
export declare class EveryOperator<T> extends IterableOperatorBase<T> {
    every(fn: (element: T, index: number) => boolean): boolean;
}
