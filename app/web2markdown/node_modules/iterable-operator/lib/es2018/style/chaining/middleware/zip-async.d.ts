import { Subject } from '../subject';
import { AsyncIterableOperator } from '../async-iterable-operator';
import { ExtractTypeTupleFromAsyncLikeIterableTuple } from '../../../middleware/zip-async';
export { ExtractTypeTupleFromAsyncLikeIterableTuple };
export declare class ZipAsyncOperator<T, U extends Iterable<T> | AsyncIterable<T>> extends Subject<U> {
    zipAsync<V extends Array<Iterable<unknown> | AsyncIterable<unknown>>>(...iterables: V): AsyncIterableOperator<[T, ...ExtractTypeTupleFromAsyncLikeIterableTuple<V>]>;
}
