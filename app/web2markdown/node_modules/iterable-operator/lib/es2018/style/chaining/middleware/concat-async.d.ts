import { Subject } from '../subject';
import { AsyncIterableOperator } from '../async-iterable-operator';
export declare class ConcatAsyncOperator<T, U extends Iterable<T> | AsyncIterable<T>> extends Subject<U> {
    concatAsync<V>(...iterables: Array<Iterable<V | PromiseLike<V>> | AsyncIterable<V>>): AsyncIterableOperator<T | V>;
}
